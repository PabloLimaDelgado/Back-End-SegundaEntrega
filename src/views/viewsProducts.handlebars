<h1>Products: </h1>
{{#each products}}
  <div class="product">
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <button>Agregar al carrito</button>
    <a href="/products/{{this._id}}">Ver detalles</a>
  </div>
{{/each}}